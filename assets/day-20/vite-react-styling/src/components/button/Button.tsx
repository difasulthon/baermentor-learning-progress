import React from "react";

import "./Button.module.css";

type Props = {};

const Button = (props: Props) => {
  return <button className="buttonComponent">Click Me</button>;
};

export default Button;
